services:
  # -------------------------------------------------------------------
  # Redis (free managed instance on Render)
  # -------------------------------------------------------------------
  - type: redis
    name: insight-redis
    plan: free
    ipAllowList: []

  # -------------------------------------------------------------------
  # Backend + Worker (combined: FastAPI + Celery in one web service)
  # Render free tier does not support background worker service type.
  # render-entrypoint.sh starts both uvicorn and celery in one container.
  # -------------------------------------------------------------------
  - type: web
    name: insight-backend
    plan: free
    runtime: docker
    dockerfilePath: ./Dockerfile.render
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: APP_NAME
        value: Dataset Insight Automator API
      - key: ENVIRONMENT
        value: production
      - key: API_V1_PREFIX
        value: /api/v1
      - key: REDIS_URL
        fromService:
          type: redis
          name: insight-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: insight-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: insight-redis
          property: connectionString
      - key: MAX_UPLOAD_SIZE_MB
        value: "10"
      - key: ALLOWED_EXTENSIONS
        value: csv,xlsx,xls
      - key: CORS_ORIGINS
        value: https://insight-frontend.onrender.com
      - key: UPLOAD_RATE_LIMIT
        value: 10/minute
      - key: STATUS_RATE_LIMIT
        value: 60/minute
      - key: GEMINI_API_KEY
        sync: false
      - key: GEMINI_MODEL
        value: gemini-1.5-flash
      - key: RESEND_API_KEY
        sync: false
      - key: RESEND_FROM_EMAIL
        value: insights@resend.dev

  # -------------------------------------------------------------------
  # Frontend (Next.js)
  # -------------------------------------------------------------------
  - type: web
    name: insight-frontend
    plan: free
    runtime: docker
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    healthCheckPath: /
    envVars:
      - key: NEXT_PUBLIC_API_BASE_URL
        value: https://insight-backend.onrender.com
